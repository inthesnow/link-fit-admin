<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkfit.admin.mapper.PaymentMethodMapper">

    <resultMap id="paymentMethodResultMap" type="PaymentMethod">
        <id     property="id"        column="id"/>
        <result property="name"      column="name"/>
        <result property="sortOrder" column="sort_order"/>
    </resultMap>

    <select id="findAll" resultMap="paymentMethodResultMap">
        SELECT * FROM payment_method ORDER BY sort_order ASC, id ASC
    </select>

    <select id="existsByName" resultType="boolean">
        SELECT COUNT(*) > 0 FROM payment_method WHERE name = #{name}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment_method (name, sort_order)
        VALUES (#{name}, (SELECT t.m FROM (SELECT COALESCE(MAX(sort_order), 0) + 1 AS m FROM payment_method) t))
    </insert>

</mapper>
