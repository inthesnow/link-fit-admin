<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkfit.admin.mapper.GymSettingMapper">

    <resultMap id="gymSettingResultMap" type="GymSetting">
        <id     property="id"          column="id"/>
        <result property="gymName"     column="gym_name"/>
        <result property="gymPhone"    column="gym_phone"/>
        <result property="gymAddress"  column="gym_address"/>
        <result property="isOpen"      column="is_open"/>
        <result property="monOpen"     column="mon_open"/>
        <result property="monClose"    column="mon_close"/>
        <result property="monClosed"   column="mon_closed"/>
        <result property="tueOpen"     column="tue_open"/>
        <result property="tueClose"    column="tue_close"/>
        <result property="tueClosed"   column="tue_closed"/>
        <result property="wedOpen"     column="wed_open"/>
        <result property="wedClose"    column="wed_close"/>
        <result property="wedClosed"   column="wed_closed"/>
        <result property="thuOpen"     column="thu_open"/>
        <result property="thuClose"    column="thu_close"/>
        <result property="thuClosed"   column="thu_closed"/>
        <result property="friOpen"     column="fri_open"/>
        <result property="friClose"    column="fri_close"/>
        <result property="friClosed"   column="fri_closed"/>
        <result property="satOpen"     column="sat_open"/>
        <result property="satClose"    column="sat_close"/>
        <result property="satClosed"   column="sat_closed"/>
        <result property="sunOpen"     column="sun_open"/>
        <result property="sunClose"    column="sun_close"/>
        <result property="sunClosed"   column="sun_closed"/>
        <result property="notice"      column="notice"/>
        <result property="updatedAt"   column="updated_at"/>
    </resultMap>

    <select id="find" resultMap="gymSettingResultMap">
        SELECT * FROM gym_setting WHERE gym_id = #{gymId}
    </select>

    <!-- id는 auto_increment가 아니라 레거시 단일 행 구조를 유지한 고정값 PK라, 신규 지점의
         첫 저장 시에는 현재 최대값+1을 직접 계산해서 넣는다. gym_id엔 UNIQUE KEY가 있어
         이미 저장된 지점은 ON DUPLICATE KEY UPDATE로 정상적으로 갱신된다. -->
    <insert id="upsert">
        INSERT INTO gym_setting (
            id, gym_id, gym_name, gym_phone, gym_address, is_open,
            mon_open, mon_close, mon_closed,
            tue_open, tue_close, tue_closed,
            wed_open, wed_close, wed_closed,
            thu_open, thu_close, thu_closed,
            fri_open, fri_close, fri_closed,
            sat_open, sat_close, sat_closed,
            sun_open, sun_close, sun_closed,
            notice
        ) VALUES (
            (SELECT next_id FROM (SELECT COALESCE(MAX(id), 0) + 1 AS next_id FROM gym_setting) AS t),
            #{gymId}, #{setting.gymName}, #{setting.gymPhone}, #{setting.gymAddress}, #{setting.isOpen},
            #{setting.monOpen}, #{setting.monClose}, #{setting.monClosed},
            #{setting.tueOpen}, #{setting.tueClose}, #{setting.tueClosed},
            #{setting.wedOpen}, #{setting.wedClose}, #{setting.wedClosed},
            #{setting.thuOpen}, #{setting.thuClose}, #{setting.thuClosed},
            #{setting.friOpen}, #{setting.friClose}, #{setting.friClosed},
            #{setting.satOpen}, #{setting.satClose}, #{setting.satClosed},
            #{setting.sunOpen}, #{setting.sunClose}, #{setting.sunClosed},
            #{setting.notice}
        )
        ON DUPLICATE KEY UPDATE
            gym_name   = #{setting.gymName},   gym_phone    = #{setting.gymPhone},
            gym_address= #{setting.gymAddress}, is_open     = #{setting.isOpen},
            mon_open   = #{setting.monOpen},   mon_close    = #{setting.monClose},   mon_closed = #{setting.monClosed},
            tue_open   = #{setting.tueOpen},   tue_close    = #{setting.tueClose},   tue_closed = #{setting.tueClosed},
            wed_open   = #{setting.wedOpen},   wed_close    = #{setting.wedClose},   wed_closed = #{setting.wedClosed},
            thu_open   = #{setting.thuOpen},   thu_close    = #{setting.thuClose},   thu_closed = #{setting.thuClosed},
            fri_open   = #{setting.friOpen},   fri_close    = #{setting.friClose},   fri_closed = #{setting.friClosed},
            sat_open   = #{setting.satOpen},   sat_close    = #{setting.satClose},   sat_closed = #{setting.satClosed},
            sun_open   = #{setting.sunOpen},   sun_close    = #{setting.sunClose},   sun_closed = #{setting.sunClosed},
            notice     = #{setting.notice}
    </insert>

    <update id="updateOpenStatus">
        UPDATE gym_setting SET is_open = #{isOpen} WHERE gym_id = #{gymId}
    </update>

</mapper>
